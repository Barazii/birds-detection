make test again, but only sample of images. For exmaple one folder. do this by 2nd arg to the funtion processing unittesting false by default. create a new entrie dataset folder with all files same but contain one class images information (boudning boxex, ...etc). Use s5cmd. Load .env file instead of monkeypatch it. 
Try more threads in processing job. Related to more processing instances / distributed processing aws? 